<!DOCTYPE html>
<html>
    <head>
        {% include '/includes/head.html.twig' %}
    </head>
    <body>
        {% include '/includes/navbar.html.twig' %}
        <div class="container">
            <div class="jumbotron">
                <h1 class="center">Blue's Barbershop</h1>
            </div>
            <h3>Are you sure you want to delete {{ stylist.getName }}?</h3>
            <p><em>This will also delete all of his or her clients.</em></p>

            <form action='/stylist/{{ stylist.getId }}' method='post'>
                <input name="_method" type="hidden" value="delete">
                <button type='submit' class='btn btn-danger'>Delete Stylist</button>
                <a href="/stylist/{{ stylist.getId }}" class="btn btn-warning" role="button">Cancel</a>
            </form>
        </div>
    </body>
</html>
