<!DOCTYPE html>
<html>
    <head>
        {% include '/includes/head.html.twig' %}
    </head>
    <body>
        {% include '/includes/navbar.html.twig' %}
        <div class="container">
            <div class="jumbotron">
                <h1 class="center">Blue's Barbershop</h1>
            </div>
            <h2>{{ client.getName }}</h2>
            <form action='/client/{{ client.getId }}' method='post'>
                <input name="_method" type="hidden" value="patch">
                <div class="form-group">
                    <label for='new-name'>Update Client's Name:</label>
                    <input name='new-name' type='text' class='form-control' required>
                </div>
                <button type='submit' class='btn btn-success'>Update Name</button>
                <a href="/client/{{ client.getId }}" class="btn btn-danger" role="button">Cancel</a>
            </form>
        </div>
    </body>
</html>
