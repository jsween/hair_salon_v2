<!DOCTYPE html>
<html>
    <head>
        {% include '/includes/head.html.twig' %}
    </head>
    <body>
        {% include '/includes/navbar.html.twig' %}
        <div class="container">
            <div class="jumbotron">
                <h1 class="center">Blue's Barbershop</h1>
            </div>
            <h2>{{ stylist.getName }}</h2>
            <form action='/stylist/{{ stylist.getId }}' method='post'>
                <input name="_method" type="hidden" value="patch">
                <div class="form-group">
                    <label for='new-name'>Enter the new name for {{  stylists.getName }}:</label>
                    <input name='new-name' type='text' class='form-control'>
                </div>
                <button type='submit' class='btn btn-success'>Submit</button>
                <a href="/stylist/{{ stylist.getId }}" class="btn btn-danger" role="button">Cancel</a>
            </form>
        </div>
    </body>
</html>
